---
"comicarr": patch
---

Regenerate API keys on the server through a dedicated endpoint. The new key is generated with a cryptographically secure source and persisted immediately, instead of being generated in the browser and only saved if you remembered to submit the settings form.
