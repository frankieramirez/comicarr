---
"comicarr": patch
---

GetComics searches find releases again. The site's search page stopped rendering results in the page itself, so every search returned zero; Comicarr now queries the site's WordPress API for the same results.
