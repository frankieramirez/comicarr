---
"comicarr": patch
---

Searching a manga series now targets the blended frontier by default — missing released volumes plus chapters beyond the last volume — with per-series volumes-only and chapters-only modes.
