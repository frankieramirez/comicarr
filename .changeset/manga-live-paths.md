---
"comicarr": patch
---

Manga series now actually refresh on the database-update schedule, search only the blended frontier (or the per-series volumes/chapters toggle), and interpret bare filenames like `Naruto 12.cbr` using the series setting on import, scan, and post-process.
