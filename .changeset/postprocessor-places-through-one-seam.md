---
"comicarr": patch
---

Route the remaining post-processor file placements through the shared placement stage, so every one of them reads the file operation setting at the moment it runs rather than from a value passed in earlier.
