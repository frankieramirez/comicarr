---
"comicarr": patch
---

The series-detail header now shows a real last-refresh time (or "unsynced" when there isn't one) and renders the series description instead of leaving every title looking unsynced.
